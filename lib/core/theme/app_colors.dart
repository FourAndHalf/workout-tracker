import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF0D0F12);
  static const Color surface = Color(0xFF161A21);
  static const Color card = Color(0xFF1C2129);
  static const Color border = Color(0xFF2A2F3A);

  // Accents
  static const Color primary = Color(0xFF34D399); // Neon green
  static const Color secondary = Color(0xFF60A5FA); // Electric blue
  static const Color tertiary = Color(0xFFA78BFA); // Purple
  static const Color warning = Color(0xFFFBBF24); // Amber
  static const Color error = Color(0xFFF87171); // Red

  // Text
  static const Color textPrimary = Color(0xFFE8EAED);
  static const Color textSecondary = Color(0xFF9AA0A6);
  static const Color textMuted = Color(0xFF5F6368);

  // Block Badges
  static const Color straightBlock = Color(0xFF9AA0A6);
  static const Color supersetBlock = Color(0xFF60A5FA);
  static const Color triSetBlock = Color(0xFFA78BFA);
  static const Color giantSetBlock = Color(0xFFF472B6);
  static const Color dropSetBlock = Color(0xFFFBBF24);
  static const Color restPauseBlock = Color(0xFF34D399);
}
